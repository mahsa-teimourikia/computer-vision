# Advanced 07 assets

These eight SVGs are rendered deterministically from the coordinate specifications in `specs/` with `scripts/render_course_diagrams.py`. Each image includes accessible title/description metadata and an opaque light-safe background.

