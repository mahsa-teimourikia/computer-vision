# Course 03 assets

These diagrams are generated deterministically from the coordinate specifications in `specs/` by `scripts/render_course_diagrams.py`.

Each SVG is repository-local, accessible, reusable in the chapter and notebook, and validated for canvas bounds, node separation, group containment, and port attachment.

```bash
make diagrams
```

