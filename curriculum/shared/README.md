# Shared curriculum resources

Place data fixtures here only after at least two lessons genuinely depend on them. All teaching code stays inside each self-contained notebook; topic-specific data and diagrams remain inside the owning lesson directory.
