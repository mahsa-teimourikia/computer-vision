# Shared assets

This directory is reserved for repository-wide brand assets and diagrams. Lesson-specific visuals belong in that lesson's `assets/` directory and must include descriptive alt text where referenced.
