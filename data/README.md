# Data

Store only small, redistributable datasets or deterministic fixtures used by multiple lessons here. Document the source, license, collection method, intended use, known limitations, and relevant privacy considerations for every dataset.

The image-fundamentals lesson generates its synthetic fixture in code, so it does not require a downloaded dataset.
